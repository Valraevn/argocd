apiVersion: v1
kind: Service
metadata:
  name: vdsm-service
  labels:
    app: vdsm
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    app: vdsm